{
  "name": "reshape-parser",
  "description": "html parser for reshape",
  "version": "0.0.0",
  "author": "Jeff Escalante",
  "ava": {
    "verbose": "true"
  },
  "bugs": "https://github.com/reshape/parser/issues",
  "dependencies": {
    "parse5": "^2.1.5",
    "when": "^3.7.7"
  },
  "devDependencies": {
    "ava": "0.15.x",
    "coveralls": "2.x",
    "nyc": "6.x",
    "standard": "7.x"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "homepage": "https://github.com/reshape/parser",
  "license": "MIT",
  "main": "lib",
  "repository": "reshape/parser",
  "scripts": {
    "coverage": "nyc ava",
    "coveralls": "nyc --reporter=lcov ava && cat ./coverage/lcov.info | coveralls",
    "lint": "standard",
    "test": "ava"
  }
}
